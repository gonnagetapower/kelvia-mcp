# Node
node_modules/
*.log
*.tsbuildinfo

# Build artifacts
dist/
coverage/

# Env
.env
.env.*
!.env.example

# Local MCP config (may contain API keys)
.mcp.json
# ...except the plugin's, which is part of the published plugin and holds only
# the public endpoint URL. Without this the sync script cannot see it, and the
# plugin ships with no server at all.
!plugin/.mcp.json

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# MCP Bundle build output
build/
